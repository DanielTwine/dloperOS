# Dloper OS Standard

Runs Dloper Panel + reverse proxy + school website placeholder.

- Services: reverse-proxy, dloper-panel, school-website
- Usage: `docker compose -f docker-compose/docker-compose.yml up --build`
- Disable ClassConnect services by commenting them out in compose if not needed.
